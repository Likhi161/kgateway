apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: gathergenieroute
  namespace: keycloak-ns

spec:
  hostnames:
    - keycloak.gathergenie.in

  parentRefs:
    - name: likhitha-kgateway   
      namespace: kgateway-ns

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /

      backendRefs:
        - name: keycloak        
          port: 8080